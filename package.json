{
  "name": "life",
  "description": "Cherish Life",
  "version": "1.1.0",
  "main": "index.js",
  "repository": "git@github.com:Echlorine/life.git",
  "author": "Echo <echo@echocolate.xyz>",
  "license": "GPL-3.0",
  "scripts": {
    "docs:dev": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs",
    "docs:build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs",
    "docs:clean-dev": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs --clean-cache"
  },
  "devDependencies": {
    "@vuepress/bundler-vite": "2.0.0-rc.14",
    "vuepress": "^2.0.0-rc.14",
    "vuepress-theme-plume": "1.0.0-rc.86"
  }
}
